# Mocks a ReSTful Endpoint

##Install
npm install -g json-server

##Start
json-server --watch db.json

##TODO
* Implement service calls against the mock data.
* Implement the grunt runner for json-server to automatically start the mock endpoint 